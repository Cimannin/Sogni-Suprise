Put your background music here as idle.mp3 (played on a loop during the finale).
If this file is missing, a built-in music-box lullaby plays instead.
To use a different name/format, change MUSIC_FILE at the top of the music section in src/audio.js.

Put your button sound clips here (the "Press me" button at the end of the birthday message).
One is picked at random each press. Currently:
  - airhorn.mp3
  - the-krusty-krab-pizza-song.mp3
If the pick is missing, a synthesized honk plays instead.
To add, remove or rename clips, edit the AIRHORN_FILES list in the airhorn section in src/audio.js.
